class Turma {
  private id: string;
  private nome: string;
  private docentes: string;
  private modulo: string;

  constructor(id: string, nome: string, docentes: string, modulo: string) {
    this.id = id;
    this.nome = nome;
    this.docentes = docentes;
    this.modulo = modulo;
  }
}
